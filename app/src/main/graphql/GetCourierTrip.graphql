query GetTrip($tripId: UUID!) {
    getTripDetails(tripId: $tripId) {
        id
        status
        route {
            polyline
        }
        cost
        start_location {
            lat
            lng
        }
    }
}